# Add Python packages to install on top of escomp/cesm-lab-neon here.
# Example:
# xarray==2024.10.0
# netcdf4

# Required so JupyterHub's DockerSpawner can launch this image as a
# single-user server (see deploy/jupyterhub/). The image stays usable
# standalone — Hub just overrides the entrypoint to jupyterhub-singleuser.
jupyterhub

# Used by analytics_modules.llm_interaction. Notebooks that don't call
# ask_llm work fine without an API key; set OPENAI_API_KEY at runtime
# (via .env at the Hub level, or per-container) to actually use it.
openai
